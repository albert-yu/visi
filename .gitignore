# Generated by Cargo
# Will contain compiled code and dependencies
/target

# OS generated files
.DS_Store
.DS_Store?
*_thumbs.db
Thumbs.db

# IDE and Editor files
.vscode/
.idea/
*.swp
*.swo

# Agent skills: `.agents/skills/` is checked in, the per-machine state is not.
.agents/settings.local.json
.agents/scheduled_tasks.lock
.claude/settings.local.json
.claude/scheduled_tasks.lock
.pi/

# Fuzzing test harness artifacts
venv
fuzz_results/
__pycache__

# Macro-enabled workbook for fuzz_pivot.py's AppleScript driver -- a build
# output, generated on first use from the checked-in fuzz/BuildFuzzPivot.bas
# by `visi macro add` (ExcelPivotDriver._ensure_macro_template).
fuzz/pivot_macro_template.xlsm

# Python extension module built from visi-python by `maturin develop`.
# `maturin develop` installs into the active venv (already ignored), but an
# in-tree or custom --target-dir build leaves artifacts here.
*.so
*.pyd
*.dylib
*.egg-info/
visi-python/target/
