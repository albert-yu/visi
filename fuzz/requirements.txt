openpyxl>=3.1.0
# Builds the `visi_core` extension module (the visi-python crate) into the
# active venv, so the fuzzers can drive the engine in-process:
#   maturin develop -m visi-python/Cargo.toml --release
maturin>=1.7
# fuzz/test_backend_parity.py and visi-python/tests/
pytest>=8.0
