//! `visi`: Command line interface for the visi spreadsheet application.

fn main() {
    // CLI handling (args, flags, I/O) will be implemented here, consuming libvisi.
    let _ = () /* libvisi entry point placeholder */;
}
